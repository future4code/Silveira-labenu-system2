export class Turma{
    public id: string;
    public nome: string;
    public modulo: string;
 
    constructor(
       id: string,
       nome: string,
       modulo: string
    ){
       this.id = id,
       this.nome = nome,
       this.modulo = modulo
    }
 }
 